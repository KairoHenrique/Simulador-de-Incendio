#pragma once

namespace Config {
    // Direcoes do vento (cima, baixo, esquerda, direita)
    const bool Vento[4] = {true, true, true, true}; 
    
    // Numero maximo de iteracoes
    const int MaxIteracoes = 1000;
    
    // Exibir simulacao no console
    const bool exibirNoConsole = true;
}